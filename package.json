{
  "scripts": {
    "test": "jest --env='jsdom' --coverage --watch",
    "test-once": "jest --env='jsdom' --coverage",
    "start": "parcel codebreaker.html",
    "build": "parcel build codebreaker.html"
  },
  "dependencies": {
    "@babel/core": "^7.15.4",
    "@babel/preset-env": "^7.15.4",
    "babel-jest": "^27.1.0",
    "jest": "^27.1.0",
    "parcel": "^2.0.0-rc.0",
    "parcel-bundler": "^1.12.5"
  }
}
